---
slug: items
---

{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id, followships: true

  assign id = context.params.slug2 | split: '-' | last
  function item = 'lib/data/queries/items/find', id: id

  include 'lib/present_or_404', property: item.id

  render 'theme/futurelife/dashboard/items/show', item: item, current_profile: current_profile, authenticity_token: context.authenticity_token
%}
